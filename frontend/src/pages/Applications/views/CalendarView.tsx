/** Calendar view — the existing calendar, embedded as-is under the
 *  Applications header (its own revamp comes later). */
import CalendarPage from "../../Calendar/Calendar.tsx";

export default function CalendarView() {
  return <CalendarPage embedded />;
}
